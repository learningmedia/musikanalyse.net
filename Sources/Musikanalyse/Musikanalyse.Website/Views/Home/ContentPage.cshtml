@model Musikanalyse.Services.Contracts.ContentPage
@using MarkdownDeep

@{
    ViewBag.Title = this.Model.Title;
}

@Html.Raw((new Markdown { ExtraMode = true, SafeMode = true, MarkdownInHtml = true }).Transform(@Model.Value))
